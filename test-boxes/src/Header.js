import React from "react";

const Header = () => {
  return (
    <header>
      <nav>
        <div>Template for function component is here from header</div>
      </nav>
    </header>
  );
};

export default Header;
